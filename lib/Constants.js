/* Global Configuration Start */

const maxThreads = 30; // Maximum number of unread threads to fetch on each execution. Keep this number low.

/* Global Configuration End */
export { maxThreads };

export default { maxThreads };
